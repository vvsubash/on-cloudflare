{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build && mv functions/ dist/",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230404.0",
    "miniflare": "^2.13.0",
    "nuxt": "npm:nuxt3@latest",
    "wrangler": "^2.13.0"
  }
}
